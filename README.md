This is a java based project that aims to simulate the functionality of the chip 8. It works by implementing necessary functions that identifies the chip 8's assigned codes and turns them into a functional prompt. This project can read the .ch8 files
and depending on the content of the file it will create it as an app.
